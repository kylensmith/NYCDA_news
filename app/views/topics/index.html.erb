<h1>Topics#index</h1>
<p>Find me in app/views/topics/index.html.erb</p>

<% @topic.each do |topic| %>
   <%= link_to (topic.category), (topic_path(topic.category)) %>
   <%= image_tag("/assets/#{article.image_url}")%>
<% end %>